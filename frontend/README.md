# frontend

Cliente CLI da Calculadora de Calorias feito com o template `app` do Leiningen.

## Usage

Com a API do backend rodando em `http://localhost:3000`, execute:

    lein run

O arquivo principal e o menu ficam em `src/frontend/core.clj`.
